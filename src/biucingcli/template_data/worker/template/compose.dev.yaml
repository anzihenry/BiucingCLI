services:
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        GOPROXY: ${GOPROXY:-https://proxy.golang.org,direct}
        GOSUMDB: ${GOSUMDB:-sum.golang.org}
    image: ${DEV_IMAGE:-{{WORKER_NAME}}-dev}:${DEV_TAG:-latest}
    working_dir: /app
    environment:
      CONFIG_FILE: configs/config.json
      WORKER_RUN_MODE: ${WORKER_RUN_MODE:-{{RUN_MODE}}}
      WORKER_TICK_INTERVAL_SECONDS: ${WORKER_TICK_INTERVAL_SECONDS:-{{TICK_INTERVAL_SECONDS}}}
      WORKER_SHUTDOWN_TIMEOUT_SECONDS: ${WORKER_SHUTDOWN_TIMEOUT_SECONDS:-{{SHUTDOWN_TIMEOUT_SECONDS}}}
      WORKER_RETRY_MAX_ATTEMPTS: ${WORKER_RETRY_MAX_ATTEMPTS:-3}
      WORKER_RETRY_INITIAL_BACKOFF_SECONDS: ${WORKER_RETRY_INITIAL_BACKOFF_SECONDS:-1}
      WORKER_RETRY_MAX_BACKOFF_SECONDS: ${WORKER_RETRY_MAX_BACKOFF_SECONDS:-30}
      GOCACHE: /cache/go-build
      GOMODCACHE: /cache/go-mod
    volumes:
      - .:/app
      - worker-go-build-cache:/cache/go-build
      - worker-go-mod-cache:/cache/go-mod
    command: sh -lc "go run ./cmd/worker"

volumes:
  worker-go-build-cache:
  worker-go-mod-cache:
