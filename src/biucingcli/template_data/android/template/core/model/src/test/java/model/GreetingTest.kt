package {{PACKAGE_NAME}}.core.model

import org.junit.Assert.assertEquals
import org.junit.Test

class GreetingTest {
    @Test
    fun greetingHoldsData() {
        val greeting = Greeting(
            title = "{{DISPLAY_NAME_KOTLIN}}",
            subtitle = "Generated by BiucingCLI",
        )

        assertEquals("{{DISPLAY_NAME_KOTLIN}}", greeting.title)
    }
}
