# Development-only example. No secrets belong in VITE_* variables.
# Node production does not auto-load .env: inject runtime environment explicitly.
SSR_PRIVATE_TOKEN=
# Production runtime only (Vite dev/preview use --host/--port instead).
HOST=0.0.0.0
PORT=3000
