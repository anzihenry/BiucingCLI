export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">BiucingCLI</p>
        <h1>{{DISPLAY_NAME}}</h1>
        <p className="description">
          A restrained React + TypeScript starter generated from your personal stack.
        </p>
      </section>
    </main>
  );
}
