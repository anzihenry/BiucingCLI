# {{DISPLAY_NAME}}

This project was generated by BiucingCLI.

## Stack

- React
- TypeScript
- Docker

## Project Layout

- `src/pages`: route-level pages or screens
- `src/router`: route declarations and top-level router composition
- `src/components`: reusable UI building blocks
- `src/hooks`: stateful view hooks
- `src/services`: API client, data access, and local fallback services
- `src/config`: runtime configuration derived from Vite environment variables
- `src/types`: shared TypeScript models

## Docker Workflow

```bash
make dev
```

This starter is designed to run development, checks, and runtime packaging inside Docker by default.

## Available Commands

```bash
make install
make dev
make dev-shell
make lint
make typecheck
make test
make build
make browser-install
make browser-smoke
make docker-build
make docker-run
```

## Local Frontend URL

- Development server: `http://127.0.0.1:5173`
- Runtime image: `http://127.0.0.1:8080`

## Direct pnpm Scripts

```bash
pnpm dev
pnpm lint
pnpm build
pnpm typecheck
pnpm test
pnpm test:watch
pnpm browser:install
pnpm browser:smoke
pnpm format
pnpm format:check
```

## Quality Checks

- ESLint runs with the flat config format and TypeScript-aware rules.
- Prettier keeps formatting consistent across the starter files.
- Vitest and Testing Library provide a fast component-test baseline.
- Playwright provides a single-command browser smoke check for localhost verification.
- `make browser-smoke` runs the Playwright smoke check inside the Docker development image.

## Runtime Configuration

- `VITE_API_BASE_URL`: optional base URL for backend requests.
- When `VITE_API_BASE_URL` is not set, the starter keeps using local fallback data.

## Docker Files

- `Dockerfile.dev`: Playwright-enabled Node development image
- `compose.dev.yaml`: local Docker development workflow
- `Dockerfile`: production build and static runtime image
- `nginx.conf`: SPA-friendly static serving config

## Package Manager

- This starter standardizes on `pnpm` via Corepack.
- The generated `package.json` declares the expected `pnpm` and Node.js versions.

## Browser Smoke Check

```bash
make browser-install
make browser-smoke
```

- `browser:smoke` starts a local Vite server on `127.0.0.1:4173`, opens Chromium, checks the starter homepage, and writes a screenshot under `test-results/`.
