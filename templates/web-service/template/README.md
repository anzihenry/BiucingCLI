# {{SERVICE_NAME}}

This project was generated by BiucingCLI.

## Stack

- Go
- Gin
- Docker

## Project Layout

- `cmd/server`: application entrypoint
- `internal/config`: config loading
- `internal/handler`: HTTP handlers
- `internal/service`: business logic
- `internal/model`: response models
- `internal/router`: route wiring
- `configs`: local config files
- `tests`: integration-style HTTP tests

## Docker Workflow

```bash
make bootstrap
make dev
```

The starter is designed to run development, checks, and runtime packaging inside Docker by default.

## Available Commands

```bash
make bootstrap
make dev
make dev-shell
make doctor
make lint
make test
make verify
make build
make docker-build
make docker-run
```

The service reads configuration from `configs/config.yaml` by default.

## Environment

- `Brewfile`: optional host workstation tool installation
- `.mise.toml`: pinned Go version for host workflows
- `Dockerfile.dev`: Go development image with `air` and `golangci-lint`
- `compose.dev.yaml`: local Docker development workflow
- `.air.toml`: hot reload config for the development container
- `scripts/bootstrap`: host-oriented setup helper when you do not use Docker
- `scripts/doctor`: local environment checks
- `make lint`: baseline static analysis with `golangci-lint`
- `make verify`: run doctor, lint, and tests together inside Docker
- the development image prewarms `go mod download` so the first Docker-based dev shell and verification flow spend less time resolving modules
- `GOPROXY` and `GOSUMDB` can be overridden on `make bootstrap`, `make dev`, and `make docker-build` when your network needs a different module proxy or checksum policy

## Example Endpoints

- `GET /healthz`: lightweight health check
- `GET /api/v1/ping`: sample versioned API endpoint
- `GET /api/v1/users`: list sample users from an in-memory repository
- `GET /api/v1/users/:id`: fetch a sample user by ID

## Docker

Start the hot-reload development container:

```bash
make dev
```

Open a shell in the development container:

```bash
make dev-shell
```

Build the runtime image:

```bash
make docker-build
```

Build with the Ubuntu runtime path:

```bash
make docker-build DOCKER_VARIANT=ubuntu
```

Build and run the Ubuntu variant end to end:

```bash
make docker-build DOCKER_VARIANT=ubuntu IMAGE={{SERVICE_NAME}}-ubuntu TAG=dev
make docker-run IMAGE={{SERVICE_NAME}}-ubuntu TAG=dev HOST_PORT={{HTTP_PORT}}
```

Run the container:

```bash
make docker-run
```

`make docker-run` starts whatever image tag you last built or specify explicitly through `IMAGE` and `TAG`.

Push a tagged image:

```bash
make docker-push IMAGE=registry.example.com/{{SERVICE_NAME}} TAG=0.1.0
```

Run with Docker Compose:

```bash
docker compose up --build
```

The Docker defaults are:

- `DOCKER_VARIANT=alpine`: `golang:1.26-alpine` + `alpine:3.20`
- `DOCKER_VARIANT=ubuntu`: `public.ecr.aws/docker/library/golang:1.26` + `public.ecr.aws/docker/library/ubuntu:26.04`

You can also override the exact images directly:

```bash
make docker-build BUILDER_IMAGE=public.ecr.aws/docker/library/golang:1.26 RUNTIME_IMAGE=public.ecr.aws/docker/library/ubuntu:26.04
```
